this app instructions is based on copilot so everything inside .github folder and we have more than file instruction read what you need

never make test and install playwright
no testing package and don`t run build you only can check with run lint , typecheck commands in root package.json to make sure its correct
